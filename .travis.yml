language: nodejs

node_js:
  - 14

cache: yarn

branches:
  only:
    - master

install: 
  - yarn install --frozen-lockfile

script:
  - yarn run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  local_dir: docs-dist
  on:
    branch: master